spring:
  application:
    name: accountApp
server:
  port: 8086
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    info:
      enabled: true

name: accounts